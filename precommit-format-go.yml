pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: go
            group:
              piped: true
              jobs:
                - name: format-go
                  glob: "*.go"
                  run: format-go {staged_files}
