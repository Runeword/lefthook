pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: yaml
            group:
              piped: true
              jobs:
                - name: format-yaml
                  glob: "*.{yml,yaml}"
                  run: format-yaml {staged_files}
