pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: nix
            group:
              piped: true
              jobs:
                - name: format-nix
                  glob: "*.nix"
                  run: format-nix {staged_files}
