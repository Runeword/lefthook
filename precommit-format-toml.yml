skip_output:
  - meta

pre-commit:
  parallel: true
  commands:
    format-toml:
      glob: "*.toml"
      priority: 1
      env:
        RUST_LOG: warn
      run: taplo format --quiet {staged_files}
