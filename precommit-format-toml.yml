pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: toml
            group:
              piped: true
              jobs:
                - name: format-toml
                  glob: "*.toml"
                  run: format-toml {staged_files}
