pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: shell
            group:
              piped: true
              jobs:
                - name: lint-shell
                  glob: "*.sh"
                  run: lint-shell {staged_files}
