pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: shell
            group:
              piped: true
              jobs:
                - name: format-shell
                  glob: "*.sh"
                  run: format-shell {staged_files}
