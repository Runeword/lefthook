skip_output:
  - meta

pre-commit:
  commands:
    format-shell:
      glob: "*.sh"
      priority: 1
      run: |
        shfmt --write --diff --indent 2 --case-indent --language-dialect posix --simplify {staged_files}
        shellharden --replace {staged_files}
