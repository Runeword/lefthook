pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: lua
            group:
              piped: true
              jobs:
                - name: format-lua
                  glob: "*.lua"
                  run: format-lua {staged_files}
