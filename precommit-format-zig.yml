pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: zig
            group:
              piped: true
              jobs:
                - name: format-zig
                  glob: "*.{zig,zon}"
                  run: format-zig {staged_files}
