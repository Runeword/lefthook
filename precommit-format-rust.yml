pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: rust
            group:
              piped: true
              jobs:
                - name: format-rust
                  glob: "*.rs"
                  run: format-rust {staged_files}
