pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: opentofu
            group:
              piped: true
              jobs:
                - name: format-opentofu
                  glob: "*.{tf,tofu,tfvars}"
                  run: format-opentofu {staged_files}
