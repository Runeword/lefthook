pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: go
            group:
              piped: true
              jobs:
                - name: lint-go
                  glob: "*.go"
                  run: lint-go
