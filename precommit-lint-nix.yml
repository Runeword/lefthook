pre-commit:
  jobs:
    - name: main
      group:
        parallel: true
        jobs:
          - name: nix
            group:
              piped: true
              jobs:
                - name: lint-nix
                  glob: "*.nix"
                  run: lint-nix {staged_files}
